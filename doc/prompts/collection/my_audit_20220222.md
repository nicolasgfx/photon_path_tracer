I compared the kd-tree (higher accuracy) with the dense grid (speed) by looking at the same rendering. we need to tweak the dense grid. lets refer to kd-tree as "B" and to dense grid as "A"

1. 
Critical: "A" does not preserve high frequence shadows. this destroys the depth of realistic renderings. high frequency shadows are observable in the NEE-only screenshot. but the indirect lighting neutralizes them. "B" manages to preserve these high frequency shadows, which might already give a direction for the reason.
possible reasons:
a) no visibility check of neighbouring photons
b) no normal check of neighbouring photons
c) tangetial space projection does not work well for high frequency shadows
d) dense grid k-neighbors search is naturally worse than the kd-tree

Measure: this needs systematical investigation: add or change unittests that simulate each separate step of the indirect lighting and compare the numerical results. also take the CPU as a 3rd input. this way, we will find out exactly the step when the dense grid deviates from the high quality approaches.

2.
Critical: the final composition (NEE+Photons) in "A" is too bright and "washed out". this is visible in the "indirect lighting" debug output (attached): "B" preserves low and high frequency shadows, the scene's spatial depth is preserved. in "B", the lighting (energy flux) appears more balanced and more realistic. the indirect lighting debug output of "B" clearly proves that. this problem is very likely related to critical finding "1."

Measure: this needs systematical investigation: add or change unittests that simulate each separate step of the indirect lighting and compare the numerical results. also take the CPU as a 3rd input. this way, we will find out exactly the step when the dense grid deviates from the high quality approaches.

3. 
Major: "A" nd "B" both show shadows on the backfacing side of smaller, spherical objects in the NEE pass. that is obviously related to surface area ( small volume in space) and high curvature. for example the light backfacing side of an apple has an circular shadow area, which is not compensated by the indirect lighting. this could be improved by more NEE samples, but my guess is that small-volume high-curvature geometry is a weak point of the current indirect lighting approach.

Measure: this needs systematical investigation: add unittests which simulate exactly such geometrical situations (eg. just a small sphere in a box with 1 wall as light source).  the backside of the sphere should have a reasonable brightness. the CPU implementation should provide ground truth results, and then both "A" and "B" are compared against this

4. "suspicious speed improvement"
Medium: "B" is >100x slower than "A", which is still disturbing me in disbelieve. Measure: compare both algorithms step-by-step and in detail and provide an explanation for why "A" is so much faster.  what I find suspicious is that the performance output of "B" shows the main cost ist NEE (85,5%). the NEE debug outputs of both "A" and "B" look almost identical (good). so why don't we have this cost in "A", which mainly addresses the speed of the photon gathering?

Measure: this needs systematical investigation. the exact sequence of steps of "A" and "B" needs to be compared and an assessment for complexity and algorithmic approach needs to be done. we need a sound explanation. maybe it is related to the critical findings?

***

data for 3. "suspicious speed improvement":
  Ray trace               4.4%
  NEE (shadow)           85.5%
  Photon gather          10.7%
  BSDF continuation       0.0%
  Overhead/other         -0.6%

