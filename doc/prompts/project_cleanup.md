- do a FULL cleanup of the
a) project files & folders (except /scenes, /output/, /doc )
b) remove unused test and test files
c) remove cached and temporary  files
d) check each code file for unused functions 
e) simplify the code as much as possible. merge files into one, if there is shared context . keep code simple and human readable. respect c++ coding standards
f) I uploaded the project to github, so we can safely restore files if needed
